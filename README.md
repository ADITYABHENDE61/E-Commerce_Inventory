# E-Commerce_Inventory
A Spring Boot REST API for managing products and categories in an e-commerce inventory system. Features JWT authentication, role-based access (ADMIN/USER), CRUD operations, pagination, sorting, validation, MySQL integration, and Postman API testing.
