# E-Commerce_Inventory
A Spring Boot REST API project for managing products and categories in an e-commerce inventory system. Implements CRUD operations, pagination, sorting, DTOs, validation, exception handling, MySQL integration, and JWT-based authentication.
